# คำแนะนำสำหรับ Claude Code

## กฎข้อแรก — ทุก PR ต้องแก้ `README.md` และ `docs/` ให้ตรงกับของจริงด้วย

**การเปลี่ยนโค้ดหรือคลังข้อสอบโดยไม่แก้เอกสาร ถือว่างานยังไม่เสร็จ** ไม่ใช่งานที่ค่อยตามเก็บทีหลัง

เอกสารของ repo นี้ไม่ได้เขียนไว้ประดับ — มันเป็นสิ่งที่คนอ่านใช้ตัดสินใจ ตัวเลขที่ล้าสมัย
อันตรายกว่าการไม่มีตัวเลขเลย เพราะคนอ่านไม่มีทางรู้ว่ามันเก่าไปแล้ว

### เช็กลิสต์ก่อนเปิด PR

ถามว่า PR นี้ทำให้อะไรในนี้เปลี่ยนไปบ้าง แล้วแก้ให้ครบ

| ถ้า PR นี้… | ต้องแก้ |
| --- | --- |
| เพิ่ม/ลบข้อสอบ | `README.md` ตารางวิชาบนสุด · ยอดรวมในบรรทัดที่ 3 · `docs/roadmap.md` ตาราง "ช่องว่างที่วัดได้" |
| เพิ่มรายวิชาใหม่ | ทุกอย่างข้างบน + ตาราง 8 กลุ่มสาระใน `roadmap.md` + สถานะเฟสใน `docs/implementation-plan.md` |
| เพิ่ม/แก้เครื่องมือใน `tools/` | ตารางไฟล์ใน `README.md` (หัวข้อ "ไฟล์ในโปรเจกต์") |
| เพิ่มขั้นตอนใน `check.sh` | จำนวนขั้นใน `README.md` · `tools/check.sh` บรรทัดแรก · `docs/olympiad-guide.md` |
| เปลี่ยนโครงข้อมูล (ฟิลด์ใหม่ · สถานะใหม่) | `README.md` หัวข้อที่เกี่ยวข้อง · `_readme` ใน `questions/topics.json` |
| เปลี่ยนพฤติกรรมหน้าเว็บที่ผู้เรียนเห็น | หัวข้อ "ความสามารถ" ใน `README.md` |
| ทำเฟสในแผนเสร็จ | `docs/implementation-plan.md` และ `docs/roadmap.md` |

### ตัวเลขที่ล้าสมัยง่ายที่สุด — ตรวจทุกครั้ง

```bash
# ยอดรวมและจำนวนวิชา
python3 -c "
import json,glob
t=sum(len(json.load(open(f,encoding='utf-8'))) for f in glob.glob('data/*.json'))
c=len(json.load(open('questions/courses.json',encoding='utf-8')))
print(f'{t:,} ข้อ · {c} รายวิชา')"

python3 tools/verify_math.py | head -2     # ยอดที่ตรวจซ้ำได้ (README หัวข้อ verify_math)
python3 tools/verify_mcq.py  | tail -2     # สถานะข้อปรนัยรายวิชา
```

> เอกสารที่เป็น**บันทึกย้อนหลัง**ไม่ต้องตามแก้ตัวเลข — `docs/coverage-plan.md`
> `docs/recommendations.md` `docs/subject-fit.md` และ `docs/implementation-plan.md`
> ประกาศตัวเองไว้ที่หัวเอกสารแล้วว่าเป็นบันทึก ณ เวลานั้น · ถ้าเพิ่มเอกสารบันทึกใหม่
> **ต้องเขียนบรรทัดประกาศแบบเดียวกันไว้ที่หัวไฟล์** ไม่งั้นคนอ่านจะเข้าใจว่าเป็นสถานะปัจจุบัน
>
> ส่วน `README.md` ทั้งฉบับ และ `roadmap.md` หัวข้อ "ช่องว่างที่วัดได้" กับ "ลำดับเฟส"
> **ต้องตรงกับของจริงเสมอ**

---

## รันอะไรก่อน commit

```bash
python3 tools/build.py      # ทุกครั้งที่แก้ questions/ หรือ index.html
bash tools/check.sh         # 6 ขั้น ต้องผ่านทั้งหมด
```

`build.py` เขียน `index.html` · `data/*.json` · `คลังข้อสอบ_ออฟไลน์.html` ใหม่ทั้งหมด
**ลืมรันแล้ว `check.sh` จะไม่ผ่าน** เพราะขั้นที่ 2 เทียบไฟล์ที่ประกอบแล้วกับคลังต้นทาง

## กับดักที่เคยทำให้เสียเวลา

**รหัสประจำข้อคิดจากแฮชของ `text` + `figure` + `answer`** — แก้โจทย์หรือเฉลยแล้วรหัสเปลี่ยน
และความก้าวหน้าที่ผู้เรียนบันทึกไว้จะหาข้อเดิมไม่เจอ · ถ้าจำเป็นต้องแก้จริง ให้บันทึก
รหัสเก่า→รหัสใหม่ลง `questions/id-moves.json` (ดู `tools/rebalance_choices.py` เป็นตัวอย่าง)

**ห้ามอ่านไฟล์ `questions/*/unit-*.json` เข้ามาแล้วเขียนกลับทั้งไฟล์** — ไฟล์ในคลังเขียนกัน
อย่างน้อยสี่รูปแบบ (ย่อหน้า 1 และ 2 · ข้อละบรรทัดเดียว · บรรทัดว่างคั่นกลุ่ม · จบ/ไม่จบด้วย
บรรทัดใหม่) เขียนกลับด้วยรูปแบบเดียวจะได้ diff บวมทั้งไฟล์จนรีวิวไม่ได้ · ให้แก้เฉพาะค่าที่
ต้องการในข้อความไฟล์ตรง ๆ แล้ว `json.loads` ตรวจว่าได้ข้อมูลตรงกับที่ตั้งใจก่อนเขียนทับ

**ห้ามเปลี่ยนคีย์ `funnymath-*` ใน `index.html`** — เก็บอยู่บนเครื่องผู้เรียนจริง
เปลี่ยนชื่อคีย์เท่ากับความก้าวหน้าและ API key ของทุกคนหายหมด

**ตัวชี้วัดต้องคัดจากเอกสารหลักสูตรจริง ห้ามเขียนจากความจำ** — `_readme` ของ
`questions/topics.json` เขียนกฎนี้ไว้ · `check_official()` จับได้แค่จำนวนไม่ตรง
จับไม่ได้ว่าข้อความคลาดเคลื่อน

## กติกาของวิชาใหม่ (ตั้งไว้ในเฟส 6)

วิชาใหม่ทุกวิชาตั้งสองอย่างนี้ใน `questions/topics.json` ตั้งแต่วันแรก

- `explain_required: true` — ทุกข้อต้องมีฟิลด์ `explain` · ตอบผิดแล้วได้แค่ "ยังไม่ถูก"
  ไม่มีความหมายกับวิชาความรู้ ไม่รู้ก็คือไม่รู้อยู่ดี
- `mcq_gate: true` — เฉลยต้องไม่เป็นตัวเลือกที่ยาวที่สุดเกิน 40% ของข้อ
  **เขียนตัวเลือกทั้งสี่ให้ยาวใกล้กันตั้งแต่แรก** ไม่งั้นต้องมาไล่ยืดตัวลวงทีหลังหลายรอบ
  (ทั้งเทคโนโลยี ม.1 และ ม.2 เริ่มที่ 70–80% เพราะเขียนเฉลยเป็นประโยคเต็ม
  แต่เขียนตัวลวงเป็นวลีสั้น)

ตัวชี้วัดที่ประเมินด้วยข้อเขียนไม่ได้โดยหลักการ (เล่นกีฬา · แสดงนาฏศิลป์ · ปฏิบัติงาน)
ตั้ง `status: "practical"` พร้อม `practical_reason` — ไม่บังคับให้มีข้อสอบ
และไม่นับเป็นงานค้าง · ดู `docs/subject-fit.md` ว่ากลุ่มสาระไหนเข้ากับรูปแบบนี้แค่ไหน

## แม่แบบที่ลอกได้

- วิชาใหม่ → `tools/gen_tech_m2.py` (ผ่านกติกาเฟส 6 ครบ)
- คำอธิบายเฉลย → `questions/tech-m3/` (202 ข้อ · เฉลี่ย 177 ตัวอักษร)
  รูปแบบคือ **บอกหลักคิดก่อน แล้วบอกว่าตัวลวงผิดเพราะอะไร** · ข้อคำนวณแสดงขั้นตอน
  พร้อมจุดที่พลาดบ่อย
