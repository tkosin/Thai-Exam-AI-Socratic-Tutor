# แผนลงมือ — ทำ ม.ต้น ให้ครบ 8 กลุ่มสาระ

แผนระดับไฟล์และบรรทัด · อ่านหลัง [`roadmap.md`](roadmap.md) (ลำดับเฟส)
และ [`subject-fit.md`](subject-fit.md) (ขอบเขตที่ตัดสินแล้วว่าทำอะไร ไม่ทำอะไร)

**ขอบเขตที่ตกลงแล้ว** — ไม่ไล่ตามตัวชี้วัดเชิงปฏิบัติ · พลศึกษา · ศิลปะเชิงปฏิบัติ ·
ทักษะฟัง/พูดทุกภาษา · การเขียนความเรียง · งานฝีมือ **ไม่ทำเป็นข้อสอบเขียน**

## 0 · ของที่พังทันทีถ้าเริ่มวิชาใหม่วันนี้

ไม่ใช่เรื่องออกแบบ แต่เป็นของที่ **`tools/check.sh` จะฟ้องตั้งแต่ข้อแรก** — ตรวจจากโค้ดจริง

| # | จุด | อาการ | สถานะ |
| ---: | --- | --- | --- |
| 1 | `validate.py` `STD_RE` รับแค่ `[คว]` | ตัวชี้วัด **ท ส พ ศ ง ต ถูกปฏิเสธทั้งหมด** — ข้อแรกของภาษาไทยก็ไม่ผ่านแล้ว | ✅ **แก้แล้ว** |
| 2 | `validate.py` `PREFIX` มี 3 วิชา | วิชาใหม่ไม่มีอักษรกำกับ ด่าน "ตัวชี้วัดต้องตรงวิชา" จึงเงียบ ไม่ได้ตรวจอะไรเลย | ✅ **แก้แล้ว** — ครบ 9 ชื่อวิชา + ด่านฟ้องถ้าเจอวิชาที่ไม่รู้จัก |
| 3 | ไม่มีอะไรบังคับ `explain` | ปล่อยไปคือได้คลังไร้คำอธิบายเพิ่มอีกเท่าตัว | ✅ **แก้แล้ว** — `explain_required` + ด่านคุณภาพ |
| 4 | `index.html` `SUBJECT_ORDER` มี 3 วิชา | วิชาใหม่ตกไปอันดับ 99 ทั้งหมด **ลำดับวิชาในหน้าแรกจะมั่ว** | ⬜ ค้าง (6.5) |
| 5 | `topics.json` มีแค่ `active`/`planned` | ตัวชี้วัดเชิงปฏิบัติไม่มีที่ลง — ตั้ง active ก็บังคับให้แต่งข้อที่ไม่ควรมี ตั้ง planned ก็โกหกว่ายังมีงานค้าง | ⬜ ค้าง (6.4) |
| 6 | ไม่มีด่านตรวจข้อปรนัย | 6 กลุ่มสาระใหม่ไม่มีอะไรให้ `verify_math.py` คิดใหม่ ปล่อยไปคือไม่มีด่านอะไรเลย | ⬜ ค้าง (6.7) |

**ทั้ง 6 ข้อคือเฟส 6** ต้องเสร็จก่อนข้อสอบข้อแรกของวิชาใหม่

> ### ✅ ทำไปแล้วในรอบนี้ — 6.1 · 6.2 · 6.3
>
> - `STD_RE` รับครบ 8 กลุ่มสาระ · `PREFIX` ครบ 9 ชื่อวิชา และ**ฟ้องเองถ้าเจอวิชาที่ไม่รู้จัก**
>   (ของเดิมเงียบ ซึ่งอันตรายกว่าฟ้องผิด)
> - `explain` ไหลผ่าน `build.py` → `data/*.json` → หน้าเว็บ · เป็นฟิลด์ทางเลือก
>   จึงไม่ทำให้ 5,175 ข้อเดิมพัง
> - `check_explain()` ใน `validate.py` — บังคับตาม `explain_required` รายวิชา ·
>   ตรวจความยาวขั้นต่ำ · **ตรวจว่าไม่ใช่การทวนตัวเลือกที่ถูก** · รายงานความคืบหน้ารายวิชา
> - หน้าเว็บแสดงคำอธิบายตอนกดตรวจ **ทั้งตอนถูกและตอนผิด โดยไม่ต้องใส่รหัสผ่าน**
> - เทสต์ใหม่ 6 ข้อใน `dom_test.cjs` + 5 เคสใน `validate.py --selftest` · รวม **334/334 ผ่าน**
> - ชุดนำร่อง: เทคโนโลยี ม.3 หน่วยที่ 1 **18 ข้อ** มีคำอธิบายครบแล้ว
>
> **ค้างต่อ** — เติมคำอธิบายให้ tech-m3 อีก 184 ข้อ แล้วเปิด `explain_required` ของวิชานั้น
> ก่อนเริ่มเฟส 7 (ไม่งั้น `tech-m1`/`tech-m2` จะมีคำอธิบายครบ แต่ `tech-m3` ไม่มี ซึ่งผู้เรียน
> จะเจอความต่างกลางกลุ่มสาระเดียวกัน)

---

## เฟส 6 — เตรียมระบบ (ไม่มีข้อสอบใหม่สักข้อ)

### 6.1 ขยาย `STD_RE` และ `PREFIX` ให้รับทั้ง 8 กลุ่มสาระ

`tools/validate.py`

```python
# ค=คณิต · ว=วิทย์/เทคโนโลยี · ท=ไทย · ส=สังคม · พ=สุขศึกษา · ศ=ศิลปะ · ง=การงานอาชีพ · ต=ต่างประเทศ
STD_RE = re.compile(r"^([ควทสพศงต] \d\.\d [มป]\.\d/\d{1,2}|-)$")

PREFIX = {"คณิตศาสตร์": "ค", "วิทยาศาสตร์": "ว", "เทคโนโลยี": "ว",
          "ภาษาไทย": "ท", "สังคมศึกษา ศาสนา และวัฒนธรรม": "ส",
          "สุขศึกษา": "พ", "ศิลปะ": "ศ", "การงานอาชีพ": "ง", "ภาษาอังกฤษ": "ต"}
```

**ต้องมีเทสต์กันการถอยหลัง** — `validate.py --selftest` มีอยู่แล้ว เพิ่มเคส: ตัวชี้วัดอักษรแปลก
(`ก 1.1 ม.1/1`) ต้องไม่ผ่าน · วิชาภาษาไทยที่ใส่ `ค 1.1` ต้องไม่ผ่าน

> **ชื่อวิชา `สุขศึกษา` ไม่ใช่ `สุขศึกษาและพลศึกษา`** — ตั้งใจ เพราะคลังไม่ครอบคลุมพลศึกษา
> ชื่อวิชาต้องบอกความจริงกับผู้เรียน ไม่ใช่ยืมชื่อกลุ่มสาระเต็มมาแล้วครอบคลุมครึ่งเดียว

### 6.2 ฟิลด์ `explain` — บังคับกับวิชาใหม่ ตั้งแต่วิชาแรก

`tools/validate.py:35` แยกเป็นสองชุด เพื่อไม่ให้ 5,175 ข้อเดิมพังทันที:

```python
FIELDS = ("subject", "grade", "unit", "uname", "sub",
          "text", "answer", "level", "std", "tag")
# วิชาที่ตั้งหลังกติกา explain — ทุกข้อต้องมีคำอธิบายวิธีคิด ไม่มีข้อยกเว้น
FIELDS_EXPLAIN = FIELDS + ("explain",)
EXPLAIN_REQUIRED = {"ภาษาไทย", "สังคมศึกษา ศาสนา และวัฒนธรรม", "สุขศึกษา",
                    "ศิลปะ", "การงานอาชีพ", "ภาษาอังกฤษ", "เทคโนโลยี"}
```

**เกณฑ์คุณภาพของ `explain`** (ตรวจด้วยสคริปต์ได้):

- ยาวอย่างน้อย ~40 ตัวอักษร — กัน `"ตอบ ก"` ที่ไม่ได้อธิบายอะไร
- **ห้ามเป็นการทวนตัวเลือกที่ถูกเฉย ๆ** — ถ้า `explain` เป็นสับเซตของข้อความตัวเลือกที่ถูก ให้ตก
- ต้องบอกว่า**ทำไมตัวลวงถึงผิด** อย่างน้อยหนึ่งตัว สำหรับข้อระดับ `ยาก` ขึ้นไป

### 6.3 แสดงคำอธิบายตอนตอบผิด — **ไม่ต้องใส่รหัสผ่าน**

`index.html:2718` `MSG.no` ตอนนี้คือทางตัน:

> ❌ ยังไม่ถูก ลองทบทวนวิธีทำอีกครั้ง — ถ้าติดจริง ๆ กด "ดูเฉลย" (ต้องใส่รหัสผ่าน)

**ข้อเสนอ: แยก "คำอธิบาย" ออกจาก "เฉลย" ให้ขาด**

| | ต้องใส่รหัสผ่าน (`ANSWER_PASSWORD`) | เหตุผล |
| --- | --- | --- |
| **คำอธิบายวิธีคิด** (`explain`) | **ไม่ต้อง** | เด็กที่ตอบผิดต้องได้เรียนรู้ทันที ไม่ใช่รอครู |
| **ตัวคำตอบ** (`answer`) | ต้อง (เหมือนเดิม) | ที่กั้นไม่ให้เผลอกดดูก่อนคิด — เจตนาเดิมของ repo |

นี่คือหัวใจของเฟสนี้ · คำอธิบายที่ล็อกไว้หลังรหัสผ่านที่นักเรียนไม่มี = ไม่มีคำอธิบาย

**เทสต์ที่ต้องเพิ่มใน `dom_test.cjs`** — ตอบผิดในข้อที่มี `explain` แล้ว
คำอธิบายต้องปรากฏโดยไม่แตะรหัสผ่าน · และตัวคำตอบต้อง **ยัง** ไม่ปรากฏ

### 6.4 สถานะ `practical` ใน `topics.json`

`validate.py:86` ขยาย enum เป็น `active` / `planned` / `practical`

- `practical` = ตัวชี้วัดมีจริงในหลักสูตร แต่ **ประเมินด้วยข้อเขียนไม่ได้โดยหลักการ**
- ด่านความครอบคลุม (`validate.py:125–147`) **ต้องไม่บังคับให้มีข้อสอบ** และ
  **ต้องไม่รายงานเป็นงานค้าง**
- `check_official()` (`validate.py:162`) **ยังนับรวมในยอดตัวชี้วัดเหมือนเดิม** —
  ความครอบคลุมจึงยังพูดความจริงว่าหลักสูตรมีกี่ตัว เราทำได้กี่ตัว
- ต้องมีฟิลด์ `practical_reason` บังคับคู่กัน เขียนเป็นข้อความว่าทำไมประเมินไม่ได้
  ("ตัวชี้วัดคือการเล่นกีฬาจริง") — กันการใช้ `practical` เป็นที่ซุกตัวชี้วัดที่แค่ทำยาก

### 6.5 ลำดับวิชาในหน้าแรก

`index.html:861` — เรียงตามลำดับกลุ่มสาระของหลักสูตร ไม่ใช่ลำดับที่เพิ่มเข้ามา

```js
const SUBJECT_ORDER = ['ภาษาไทย', 'คณิตศาสตร์', 'วิทยาศาสตร์', 'เทคโนโลยี',
                       'สังคมศึกษา ศาสนา และวัฒนธรรม', 'สุขศึกษา',
                       'ศิลปะ', 'การงานอาชีพ', 'ภาษาอังกฤษ'];
```

### 6.6 ป้ายความครอบคลุมต้องพูดความจริง

วิชาที่ครอบคลุมแค่บางสาระ **ต้องบอกผู้เรียนบนการ์ดวิชาในหน้าแรก** ว่าอะไรไม่อยู่ในคลัง เช่น

> สุขศึกษา ม.2 · 250 ข้อ — **ไม่รวมพลศึกษา (การเคลื่อนไหวและกีฬา)**

อ่านจาก `topics.json` โดยตรง: สาระที่มีแต่หัวข้อ `practical` ทั้งสาระ = สาระที่ไม่ครอบคลุม
ไม่ต้องเขียนข้อความซ้ำด้วยมือให้หลุดกัน

**เหตุผล** — ป้าย "ครบทุกตัวชี้วัด" บนวิชาที่ครอบคลุมครึ่งเดียวทำให้เด็กเตรียมสอบผิด
ซึ่งเสียหายกว่าการไม่มีวิชานั้นเลย

### 6.7 ด่านตรวจข้อปรนัย — `tools/verify_mcq.py` (ไฟล์ใหม่)

6 กลุ่มสาระใหม่ไม่มีอะไรให้คิดเลขใหม่ ด่านจึงต้องเป็นการตรวจที่ **ไม่ต้องรู้ว่าคำตอบไหนถูก**
— ทั้งหมดนี้เขียนเป็นสคริปต์ได้จริงและรันใน `check.sh` ได้:

| ด่าน | จับอะไร |
| --- | --- |
| ตัวลวงที่ถูกด้วย | ตัวเลือกที่ถูกมากกว่าหนึ่ง — เป็นข้อที่**ผิด** ไม่ใช่ข้อที่ยาก (ต้องใช้พี่หลวงช่วยติดธง) |
| ตัวลวงซ้ำ | ตัวเลือกสองตัวที่ข้อความเหมือน/พ้องกัน |
| สมดุลตำแหน่งเฉลย | เฉลยกองที่ ก/ข เกินสัดส่วน — ทั้งรายหน่วยและทั้งวิชา |
| ความยาวตัวเลือก | เฉลยยาวกว่าตัวลวงอย่างมีระบบ = เดาได้โดยไม่ต้องรู้เนื้อหา |
| โจทย์เกือบซ้ำ | ขยายด่านซ้ำเดิมให้จับข้อความใกล้เคียง ไม่ใช่แค่ตรงกันเป๊ะ |
| คุณภาพ `explain` | ตามเกณฑ์ข้อ 6.2 |

**ทั้งหมดเป็นด่านสถิติ/โครงสร้าง ไม่ตัดสินความถูกของเนื้อหา** — ความถูกของเนื้อหาให้พี่หลวง
อ่านทวนแล้ว**ติดธงไว้ให้คนดู** ไม่ใช่ตัดสินอัตโนมัติ (`verify_math.py` มี `selftest()` เป็นแบบอย่าง
ให้ทำตาม: ป้อนเคสปลอมแล้วยืนยันว่าจับได้จริง ก่อนตรวจของจริงทุกครั้ง)

### สรุปเฟส 6

| ไฟล์ | งาน |
| --- | --- |
| `tools/validate.py` | 6.1 · 6.2 · 6.4 + เคส `--selftest` ใหม่ |
| `tools/verify_mcq.py` | **ใหม่** — 6.7 พร้อม `selftest()` |
| `tools/check.sh` | เพิ่มขั้นที่ 6 เรียก `verify_mcq.py` |
| `index.html` | 6.3 · 6.5 · 6.6 |
| `tools/dom_test.cjs` | เทสต์ 6.3 และ 6.6 |
| `questions/topics.json` | `_readme` อธิบายสถานะ `practical` |
| `docs/` | อัปเดต `roadmap.md` · `recommendations.md` (ปิดข้อ "ช่องว่างการเรียนรู้") |

**ไม่มีข้อสอบใหม่ในเฟสนี้** และ 327 เทสต์เดิมต้องยังผ่านครบ

---

## เฟส 7–10 — เนื้อหา

ทุกวิชาใช้ขั้นตอนเดียวกัน (ทำตาม `tech-m3` ซึ่งผ่านมาแล้ว):

1. คัดตัวชี้วัดรายชั้นจากเอกสาร สพฐ. ฉบับปรับปรุง 2560 **ตรงตัว** ลง `topics.json`
   พร้อมฟิลด์ `official` (ยอดรวม · สาระที่มี · สาระที่ยืนยันว่าไม่มี) —
   `_readme` ของไฟล์นั้นห้ามเดาไว้ล่วงหน้า ต้องเทียบตอนลงมือ
2. ตั้งหัวข้อที่ประเมินด้วยข้อเขียนไม่ได้เป็น `practical` พร้อม `practical_reason`
3. เขียน `tools/gen_<slug>.py` ออกข้อสอบ + `explain` ทุกข้อ
4. เพิ่มวิชาใน `questions/courses.json` · สร้าง `questions/<slug>/unit-NN-*.json`
5. `python3 tools/build.py` แล้ว `bash tools/check.sh` ต้องผ่านทุกขั้น

| เฟส | วิชา | slug | ประเมินจำนวนข้อ | หมายเหตุขอบเขต |
| ---: | --- | --- | ---: | --- |
| **7** | เทคโนโลยี ม.1 · ม.2 | `tech-m1` `tech-m2` | ~400 | เต็มกลุ่ม — ปิดกลุ่มสาระที่ 3 ได้ทั้งกลุ่ม |
| **8** | ภาษาไทย ม.1–ม.3 | `thai-m1..m3` | ~1,350 | **การอ่าน (ท 1.1) ก่อน** → หลักภาษา (ท 4.1) → วรรณคดีเชิงข้อเท็จจริง · ฟัง/ดู/พูด = `practical` |
| **8** | สังคมศึกษาฯ ม.1–ม.3 | `social-m1..m3` | ~1,800 | ครบ 5 สาระ · ข้อเชิงคุณค่า/ตีความไม่ทำ |
| **9** | ภาษาอังกฤษ ม.1–ม.3 | `english-m1..m3` | ~1,200 | อ่าน/ศัพท์/ไวยากรณ์/functional · ฟัง–พูด = `practical` |
| **10** | สุขศึกษา ม.1–ม.3 | `health-m1..m3` | ~750 | ไม่รวมพลศึกษา (พ 3.1 · พ 3.2 = `practical`) |
| **10** | การงานอาชีพ ม.1–ม.3 | `career-m1..m3` | ~540 | ความรู้เชิงกระบวนการ + การอาชีพ · งานฝีมือ = `practical` |
| **10** | ศิลปะ ม.1–ม.3 | `art-m1..m3` | ~360 | **เฉพาะองค์ประกอบศิลป์ · ทฤษฎีสี · อ่านโน้ต** ที่วาดด้วย SVG ได้ · ที่เหลือ `practical` |

**รวม ~6,400 ข้อ · 20 รายวิชา**

### ทำไม ท 1.1 การอ่าน มาก่อนทุกอย่างในเฟส 8

เป็นทักษะเดียวที่ส่งผลข้ามทุกวิชา — เด็กที่อ่านโจทย์คณิตไม่เข้าใจ ไม่ได้แพ้เพราะคณิต
และเป็นสาระที่เข้ากับรูปแบบ "อ่านแล้วตอบ" ตรงตัวที่สุดในทั้ง 8 กลุ่มสาระ

### ข้อควรระวังที่ต้องเขียนไว้ในหัว generator ของวิชานั้น ๆ

| วิชา | ข้อควรระวัง |
| --- | --- |
| สังคม — ศาสนา (ส 1.1/1.2) | **ข้อเท็จจริงเท่านั้น** ห้ามข้อที่ตัดสินว่าความเชื่อแบบไหนถูก — ห้องเรียนมีเด็กหลายศาสนา |
| สังคม — หน้าที่พลเมือง | กฎหมาย/รัฐธรรมนูญเปลี่ยนได้ ต้องมีวันที่กำกับในแหล่งอ้างอิง |
| สังคม — ประวัติศาสตร์ | แยกข้อเท็จจริงออกจากการตีความ · ข้อที่ถามว่าเหตุการณ์ "ดี" ไหม ไม่ควรมีเฉลยเดียว |
| สุขศึกษา — พ 2.1 | เพศศึกษาต้องเป็น**ข้อมูลสุขภาพที่เป็นกลาง** ไม่ใช่ข้อที่ตัดสินศีลธรรม |
| ภาษาไทย — วรรณคดี | จัดชั้นให้ถูก · `olympiad-guide.md` เตือนไว้แล้วเรื่องอิเหนาที่เป็นของ ม.4 |
| ศิลปะ | **ห้ามฝังภาพผลงานจริง** — ติดลิขสิทธิ์ · ใช้ SVG วาดแผนภาพเชิงหลักการเท่านั้น |

---

## ลำดับที่แนะนำให้เริ่ม

**เฟส 6 ข้อ 6.1–6.3 ก่อน** (`STD_RE` + `PREFIX` + `explain` + แสดงคำอธิบายตอนตอบผิด)
เป็นก้อนที่เล็กที่สุดที่ปลดล็อกทุกอย่างหลังจากนั้น และข้อ 6.3 อย่างเดียว
**ทำให้ 5,175 ข้อที่มีอยู่แล้วสอนเก่งขึ้นทันทีที่เติม `explain` เข้าไป** โดยไม่ต้องรอวิชาใหม่เลย
